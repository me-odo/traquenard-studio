/**
 * Exact-path exceptions to the handwritten production source hard ceiling.
 * Each entry requires a durable reason and may link temporary debt to an issue.
 */
export const sourceShapeExceptions = {};
