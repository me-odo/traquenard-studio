# Runtime authority

Sessions execute immutable validated Game Artifacts. The server owns outcomes and ordered events. Preserve explicit RNG/logical time, retry-safe command IDs, global pause for required disconnects, and server-side audience filtering. Runtime must not import authoring or UI packages.
