# Web projection

The web app never decides authoritative outcomes and never receives private payloads for another participant. CSS is not access control. Keep editor layout/view models separate from canonical IR and derive compatibility from authoring-domain types. Every drag path needs a tap/click alternative; preserve semantic HTML and keyboard use.
